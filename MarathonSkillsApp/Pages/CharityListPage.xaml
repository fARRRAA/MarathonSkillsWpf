<Page x:Class="MarathonSkillsApp.Pages.CharityListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="Charity List Page">
    <DockPanel LastChildFill="True">
        <!-- Верхняя панель -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Background="#FF241D70" Height="50">
            <Button x:Name="Back_btn" Click="Back_btn_Click" Content="Назад" Width="100" Height="30" Margin="10,10,0,10" Background="#FDC100" Foreground="White"/>
            <TextBlock Text="MARATHON SKILLS 2016" FontSize="18" FontWeight="Bold" Foreground="White"
                       VerticalAlignment="Center" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Нижняя панель -->
        <Grid DockPanel.Dock="Bottom" Background="#FF241D70" Height="30">
            <TextBlock x:Name="CountdownTextBlock" FontSize="14"
                       Foreground="#E0E0E0" VerticalAlignment="Center" Margin="10,0,0,0" TextAlignment="Center"/>
        </Grid>

        <!-- Основное содержимое -->
        <ScrollViewer Background="White">
            <StackPanel x:Name="CharityListPanel" Background="White" Margin="10,0,0,0">
                <!-- Заголовок -->
                <TextBlock Text="Список благотворительных организаций" FontSize="24" FontWeight="Bold"
                           Foreground="Black" HorizontalAlignment="Center" Margin="0,20"/>
                <TextBlock Text="Это - список всех благотворительных учреждений, которые поддерживаются в Marathon Skills 2016. Спасибо за помощь вы поддерживаете их, спонсируя бегунов!"
                           FontSize="14" Foreground="Black" TextAlignment="Center"
                           HorizontalAlignment="Center" Margin="0,-10,0,20" TextWrapping="Wrap" Width="679"/>

                <!-- Список благотворительных организаций -->
                <StackPanel>
                    
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Page>
